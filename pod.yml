apiVersion: v1
kind: Pod
metadata:
  # Here name is a core property
  name: static-web
  # Here labels is additional property
  labels:
    role: myrole
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
